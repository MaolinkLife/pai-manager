# Media Fixtures

Put reusable system-test media here.

Suggested layout:
- `images/` for PNG/JPEG vision fixtures.
- `audio/` for WAV/MP3/OGG speech and TTS fixtures.
- `video/` for short video fixtures.

Keep committed fixtures small. Larger local-only files should stay ignored unless a test explicitly needs them.
